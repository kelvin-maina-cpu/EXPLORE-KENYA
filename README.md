# Explore Kenya - Expo 54.0.33 Setup Complete

## Run Commands

**1. Navigate to frontend:**

```bash
cd Frontend-web-backup
```

**2. Start development server:**

```bash
npx expo@54.0.33 start --clear
```

**Options after bundling:**

- `a` → Android
- `i` → iOS
- `w` → Web
- Scan QR code with Expo Go app

**Notes:**

- Expo locked to ~54.0.33 (no auto-upgrade to 55)
- All errors fixed (Babel, icon, router, cache)
- TypeScript prompt: Press `n` or ignore (JS project, deps excluded)
- Peer warnings safe to ignore
